dependencies:
    override:
        - npm install --global eslint babel-eslint eslint-plugin-flowtype flow-bin

test:
    override:
        - eslint --ext js,jsx -c .eslintrc.js ./
        - flow check
